#ifndef H_PATR_INSTANCE
#define H_PATR_INSTANCE

//#include "proto.h"


/**
 * parse the entry to transform it into options
 * @param options the line of command that has been passed to traceroute instance
 * @brief idea is to be able to pass to each traceroute instance we want to create a line of option that it will treat with getopt and trnasform into options list
 */

//int init(char* protocol);

#endif
